import React, { Component } from 'react';
export default class socketPage extends Component {
  constructor(props) {
    super(props);
  }
  render() {
    return (
      <div>
        socket
        socket
        socket
        socket
        socket
        socket
      </div>
    )
  }
}